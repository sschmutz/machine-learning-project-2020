function [DTM, dictionary] = document_term_matrix (dataset, exclude_stop_words)
% TODO: this function should return a document term matrix and dictionary
% of a table. If exclude_stop_words is TRUE, those will be removed

end